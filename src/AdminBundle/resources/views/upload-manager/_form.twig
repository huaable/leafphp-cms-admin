{#{{ app.html.dropDownList('UploadManager[status]', app.request.get('UploadManager').status|default(entity.status), entity.statusAlias(true), {'class':'form-control', 'prompt': '请选择'}) }}#}

<input type="hidden" name="_token" value="{{ csrf_token()}}">

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-model">{{ entity.labels().model }}</label>
    <div class="col-sm-8">
                <input type="text" name="UploadManager[model]" class="form-control"
               value="{{ app.request.get('UploadManager').model|default(entity.model) }}" id="input-model" >
                <span class="help-block">{# 帮助信息 #}</span>
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-model_id">{{ entity.labels().model_id }}</label>
    <div class="col-sm-8">
                <input type="text" name="UploadManager[model_id]" class="form-control"
               value="{{ app.request.get('UploadManager').model_id|default(entity.model_id) }}" id="input-model_id" >
                <span class="help-block">{# 帮助信息 #}</span>
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-filename">{{ entity.labels().filename }}</label>
    <div class="col-sm-8">
                <input type="text" name="UploadManager[filename]" class="form-control"
               value="{{ app.request.get('UploadManager').filename|default(entity.filename) }}" id="input-filename" >
                <span class="help-block">{# 帮助信息 #}</span>
    </div>
</div>

<div class="form-group">
    <label class="col-sm-2 control-label" for="input-status">{{ entity.labels().status }}</label>
    <div class="col-sm-8">
                <input type="text" name="UploadManager[status]" class="form-control"
               value="{{ app.request.get('UploadManager').status|default(entity.status) }}" id="input-status" >
                <span class="help-block">{# 帮助信息 #}</span>
    </div>
</div>

